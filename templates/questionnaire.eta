<% it.questions.forEach((q) => { -%>
<%~ include("question-row", {
  id: q.id,
  text: q.text,
  scale: q.scale,
  borderline: q.borderline,
  ratings: it.ratingsDesc,
  name: `q${q.id}`,
  ariaLabel: `${it.strings.scaleQuestion} — #${q.id}`
}) %>
<% }) -%>
